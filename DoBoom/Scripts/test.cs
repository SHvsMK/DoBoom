using UnityEngine;
using System.Collections;

public class test : DoBoom {

	/*private int n;
	// Use this for initialization
	void Start () {
		if (number [0] == 0) {
			number [0] = 1;
			n = 1;
		}
		else
			n = 2;
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void OnGUI () {
		if (n == 1) {
			GUI.Label (new Rect (10, 10, 100, 20), "num" + n.ToString ());
		} else {
			GUI.Label (new Rect (101, 101, 100, 20), "num" + n.ToString());
		}

	}*/
}
